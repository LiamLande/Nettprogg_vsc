[package]
name = "protocol"
description = "Wire protocol types shared between the LiveShare Lite relay/signaling servers and clients."
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "protocol"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
crdt-core = { workspace = true }
